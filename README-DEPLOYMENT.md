# SendWalk 部署文档索引

本文档提供 SendWalk 项目部署到生产环境的完整指南。

## 📚 文档目录

### **主要文档**

1. **[生产环境部署指南.md](./生产环境部署指南.md)** ⭐
   - 完整的生产环境部署步骤
   - 服务器环境配置
   - Nginx、Supervisor 配置
   - SSL 证书配置
   - 故障排查指南
   - **适合**: 首次部署或需要详细步骤

2. **[部署快速指南.md](./部署快速指南.md)** 🚀
   - 5 分钟快速部署
   - 简化的部署步骤
   - 常见问题速查
   - **适合**: 有经验的用户或快速部署

3. **[docs/deployment.md](./docs/deployment.md)**
   - 原有的部署文档
   - 包含 Docker 部署方式
   - 本地开发环境配置
   - **适合**: 了解多种部署方式

## 🛠️ 配置文件

### **Nginx 配置**
- `nginx/api.conf` - 后端 API Nginx 配置
- `nginx/frontend.conf` - 前端 Nginx 配置

### **Supervisor 配置**
- `supervisor/scheduler.conf` - 调度器配置
- `supervisor/worker.conf` - Worker 管理器配置

### **环境配置**
- `config.env.production.example` - 生产环境配置示例

## 🚀 自动化脚本

### **部署脚本**

```bash
# 一键部署更新（在服务器上执行）
./deploy.sh production
```

功能：
- ✅ 拉取最新代码
- ✅ 更新依赖
- ✅ 运行数据库迁移
- ✅ 清除和重建缓存
- ✅ 构建前端
- ✅ 设置权限
- ✅ 重启服务
- ✅ 验证部署

### **检查脚本**

```bash
# 检查部署状态（在服务器上执行）
./check-deployment.sh
```

功能：
- ✅ 检查系统服务状态
- ✅ 检查项目目录和文件
- ✅ 检查文件权限
- ✅ 检查 Supervisor 进程
- ✅ 检查数据库和 Redis 连接
- ✅ 检查端口监听
- ✅ 检查日志文件
- ✅ 检查磁盘空间
- ✅ 检查 SSL 证书
- ✅ 测试 API 健康

## 📋 部署流程

### **首次部署**

1. **准备服务器**
   ```bash
   # 运行环境配置脚本（见《生产环境部署指南.md》步骤1）
   ./setup-server.sh
   ```

2. **部署代码**
   ```bash
   cd /var/www
   git clone https://github.com/your-username/sendwalk.git
   ```

3. **配置后端**
   ```bash
   cd /var/www/sendwalk/backend
   composer install --optimize-autoloader --no-dev
   cp .env.example .env
   # 编辑 .env 配置
   php artisan key:generate
   php artisan migrate --force
   php artisan config:cache
   ```

4. **配置前端**
   ```bash
   cd /var/www/sendwalk/frontend
   npm install
   npm run build
   ```

5. **配置 Nginx**
   ```bash
   cp nginx/api.conf /etc/nginx/sites-available/sendwalk-api
   cp nginx/frontend.conf /etc/nginx/sites-available/sendwalk-frontend
   ln -s /etc/nginx/sites-available/sendwalk-api /etc/nginx/sites-enabled/
   ln -s /etc/nginx/sites-available/sendwalk-frontend /etc/nginx/sites-enabled/
   nginx -t && systemctl restart nginx
   ```

6. **配置 Supervisor**
   ```bash
   cp supervisor/scheduler.conf /etc/supervisor/conf.d/sendwalk-scheduler.conf
   cp supervisor/worker.conf /etc/supervisor/conf.d/sendwalk-worker-manager.conf
   supervisorctl reread && supervisorctl update
   supervisorctl start all
   ```

7. **配置 SSL**
   ```bash
   certbot --nginx -d api.sendwalk.com
   certbot --nginx -d www.sendwalk.com -d sendwalk.com
   ```

8. **验证部署**
   ```bash
   ./check-deployment.sh
   ```

### **日常更新**

```bash
# SSH 到服务器
ssh user@your_server

# 切换到项目目录
cd /var/www/sendwalk

# 运行部署脚本
./deploy.sh production

# 验证部署
./check-deployment.sh
```

## 📊 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户浏览器                              │
│                   https://www.sendwalk.com                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                         Nginx (443)                         │
│  ┌──────────────────────┐      ┌──────────────────────┐   │
│  │  Frontend (SPA)      │      │  Backend API         │   │
│  │  /frontend/dist      │      │  Laravel 11          │   │
│  └──────────────────────┘      └──────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    PHP-FPM 8.3 + Laravel                    │
│  ┌──────────────────────┐      ┌──────────────────────┐   │
│  │  API Controllers     │      │  Services            │   │
│  └──────────────────────┘      └──────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       Supervisor                            │
│  ┌──────────────────────┐      ┌──────────────────────┐   │
│  │  Scheduler           │      │  Worker Manager      │   │
│  │  (schedule:work)     │      │  (manage-workers)    │   │
│  └──────────────────────┘      └──────────────────────┘   │
│                              │                             │
│                              ▼                             │
│  ┌─────────────────────────────────────────────────┐     │
│  │  Campaign Workers (campaign:process-queue)      │     │
│  │  (动态扩缩容: 1-4 个)                              │     │
│  └─────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层                                │
│  ┌──────────────────┐  ┌──────────────┐  ┌──────────┐     │
│  │  MySQL 8.0       │  │  Redis 7     │  │  Files   │     │
│  │  (数据库)         │  │  (缓存/队列)   │  │  (日志)   │     │
│  └──────────────────┘  └──────────────┘  └──────────┘     │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 自动化任务

系统配置了以下自动化任务（通过 Scheduler 执行）：

1. **队列清理** - 每天 02:00
   - 清理已完成的队列任务

2. **日志文件清理** - 每天 03:00
   - 清理超过 30 天的日志文件

3. **SendLog 清理** - 每天 04:00
   - 清理超过 30 天的发送记录

4. **定时活动处理** - 每分钟
   - 检查并启动定时发送的活动

## 📝 维护任务

### **日常检查**

```bash
# 每天执行一次
./check-deployment.sh
```

### **查看日志**

```bash
# Laravel 日志
tail -f /var/www/sendwalk/backend/storage/logs/laravel-$(date +%Y-%m-%d).log

# Scheduler 日志
tail -f /var/www/sendwalk/backend/storage/logs/scheduler.log

# Worker Manager 日志
tail -f /var/www/sendwalk/backend/storage/logs/manager.log

# Nginx 错误日志
tail -f /var/log/nginx/sendwalk-api-error.log
```

### **数据库备份**

```bash
# 手动备份
mysqldump -u sendwalk -p sendwalk | gzip > backup_$(date +%Y%m%d).sql.gz

# 配置自动备份（见《生产环境部署指南.md》）
```

### **更新系统**

```bash
# 定期更新系统软件包
apt update && apt upgrade -y
```

## 🆘 故障排查

### **快速诊断**

```bash
# 1. 检查所有服务状态
./check-deployment.sh

# 2. 查看 Supervisor 进程
supervisorctl status

# 3. 查看最新错误日志
tail -50 /var/www/sendwalk/backend/storage/logs/laravel-$(date +%Y-%m-%d).log
```

### **常见问题**

| 问题 | 解决方案 |
|------|---------|
| 502 Bad Gateway | 重启 PHP-FPM: `systemctl restart php8.3-fpm` |
| 500 Internal Server Error | 查看 Laravel 日志，检查权限 |
| Worker 不工作 | 重启 Supervisor: `supervisorctl restart all` |
| 前端无法访问 API | 检查 CORS 配置和 Nginx 配置 |

详细的故障排查指南见《生产环境部署指南.md》。

## 📞 技术支持

### **文档位置**

- 生产部署: `/生产环境部署指南.md`
- 快速部署: `/部署快速指南.md`
- Docker 部署: `/docs/deployment.md`
- API 文档: `/docs/api.md`
- 数据库架构: `/docs/database-schema.md`

### **日志位置**

- Laravel: `/var/www/sendwalk/backend/storage/logs/`
- Nginx: `/var/log/nginx/`
- Supervisor: `/var/log/supervisor/`

### **配置位置**

- 后端: `/var/www/sendwalk/backend/.env`
- Nginx: `/etc/nginx/sites-available/`
- Supervisor: `/etc/supervisor/conf.d/`

## ✅ 检查清单

部署完成后，确认以下项目：

- [ ] 所有系统服务运行正常
- [ ] 数据库连接正常
- [ ] Redis 连接正常
- [ ] Supervisor 进程运行正常
- [ ] Nginx 配置正确
- [ ] SSL 证书配置完成
- [ ] 前端可以访问
- [ ] 后端 API 可以访问
- [ ] 可以登录和使用功能
- [ ] Worker 正常处理任务
- [ ] 调度器正常运行
- [ ] 日志正常记录
- [ ] 自动清理任务配置完成
- [ ] 数据库备份配置完成
- [ ] 监控配置完成（可选）

## 🎯 下一步

部署完成后：

1. **创建管理员账号**
2. **配置 SMTP 服务器**
3. **创建第一个邮件列表**
4. **创建第一个邮件活动**
5. **配置监控和告警**
6. **设置数据库备份**
7. **添加团队成员**

---

## 📖 更多资源

- [Laravel 官方文档](https://laravel.com/docs)
- [Nginx 配置指南](https://nginx.org/en/docs/)
- [Supervisor 文档](http://supervisord.org/)
- [Let's Encrypt 文档](https://letsencrypt.org/docs/)

---

**祝您部署顺利！** 🚀

如有问题，请参考详细的《生产环境部署指南.md》或查看相关日志文件。

